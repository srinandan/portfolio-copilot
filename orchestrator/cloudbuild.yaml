steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/portfolio-copilot-orchestrator', '-f', 'orchestrator/Dockerfile', '.']

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/portfolio-copilot-orchestrator']

  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'portfolio-copilot-orchestrator'
      - '--image'
      - 'gcr.io/$PROJECT_ID/portfolio-copilot-orchestrator'
      - '--region'
      - 'us-central1'
      - '--set-labels=app=portfolio-copilot,component=orchestrator'
      - '--no-allow-unauthenticated'

images:
  - 'gcr.io/$PROJECT_ID/portfolio-copilot-orchestrator'
