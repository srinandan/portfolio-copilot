name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  python:
    name: Python
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: uv sync --project orchestrator

      - name: Lint with ruff
        run: uv run --project orchestrator ruff check .

      - name: Run tests with coverage
        env:
          PYTHONPATH: .
        run: uv run --project orchestrator pytest --cov=orchestrator/src --cov-report=term

      - name: Run simulated revocation demo
        env:
          PYTHONPATH: orchestrator/src
        run: uv run --project orchestrator python scripts/demo_live_revocation.py --mock-registry

  go:
    name: Go
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Verify schema sync
        run: ./scripts/sync-schemas.sh --check

      - name: Start Firestore emulator
        run: |
          docker run -d --name firestore-emulator -p 8080:8080 gcr.io/google.com/cloudsdktool/cloud-sdk gcloud emulators firestore start --host-port=0.0.0.0:8080
          sleep 5

      - name: Build, vet, test with coverage
        env:
          FIRESTORE_EMULATOR_HOST: localhost:8080
        run: |
          go build ./...
          go vet ./...
          go test ./... -coverprofile=coverage.out
          go tool cover -func=coverage.out

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check for package.json
        id: check
        run: |
          if [ -f "frontend/package.json" ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/setup-node@v4
        if: steps.check.outputs.exists == 'true'
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install, build, test with coverage
        if: steps.check.outputs.exists == 'true'
        working-directory: frontend
        run: |
          npm ci
          npm run build
          npm run test -- --coverage

      - name: Skip (no package.json yet)
        if: steps.check.outputs.exists == 'false'
        run: echo "No package.json in frontend/ yet — skipping."
