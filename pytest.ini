[pytest]
pythonpath = orchestrator orchestrator/src .
testpaths = orchestrator/tests evals
markers =
    eval: marks tests that evaluate LLM output quality
    live_llm: marks tests that call live LLM endpoints
